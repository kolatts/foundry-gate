---
title: System Overview
description: How FoundryGate's components fit together — quota resolution, APIM enforcement, Entra sync, and usage tracking.
---

import { Aside } from '@astrojs/starlight/components';
import ArchDiagram from '../../../components/ArchDiagram.astro';
import Pipeline from '../../../components/Pipeline.astro';
import QuotaChain from '../../../components/QuotaChain.astro';
import KeyLifecycle from '../../../components/KeyLifecycle.astro';

FoundryGate is a governance and access-management layer that sits in front of your existing Azure AI Foundry deployment. It does not replace APIM or Foundry — it adds per-developer budget enforcement, individual key lifecycle management, and Entra-driven provisioning on top of them.

## Component diagram

<ArchDiagram />

---

## Entra ID sync — how access follows your org chart

FoundryGate uses the Microsoft Graph API (app-only credentials stored in Key Vault) to keep developer access in sync with your Entra directory. This is how access governance works without any manual admin work after initial setup.

### First login — automatic provisioning

When a developer signs in through the Blazor UI for the first time, `GET /users/me` triggers the full provision pipeline:

<Pipeline
  label="First login — provision pipeline"
  steps={[
    {
      title: "Extract Entra object ID from the bearer token",
      detail: "The oid claim from the validated JWT identifies the developer uniquely.",
    },
    {
      title: "Look up developer in the FoundryGate database",
      detail: "Query Users table by Entra object ID.",
    },
    {
      title: "If not found — fetch from Graph and create user record",
      detail: "Call Graph GET /users/{oid} to fetch displayName, mail, employeeId. INSERT User row and resolve monthly token quota (five-level resolution).",
      code: "Graph GET /users/{oid}\n  → INSERT User row\n  → resolve monthly token quota",
    },
    {
      title: "Create APIM subscription via Management API",
      detail: "Call APIM Management: POST /subscriptions, scoped to /products/foundrygate. Name: foundrygate-{userId}.",
      code: "APIM Management API\n  POST /subscriptions\n    name:  foundrygate-{userId}\n    scope: /products/foundrygate",
    },
    {
      title: "Store encrypted APIM key and create quota allocation",
      detail: "Key is wrapped with a Key Vault key before writing to SQL. INSERT QuotaAllocation for the current billing period.",
    },
    {
      title: "Return UserProfileDto",
      detail: "Response includes the masked key and current quota usage. The developer is ready to call Foundry.",
    },
  ]}
/>

The developer never needs to contact an admin. Their key is ready the moment they first open the app.

### Entra group sync — bulk provisioning and deprovisioning

Admins can link a FoundryGate group to an Entra group ID. Running `POST /groups/{id}/sync-entra` (or triggering from the UI) calls:

<Pipeline
  label="Entra group sync"
  steps={[
    {
      title: "Fetch current group members from Microsoft Graph",
      code: "Graph GET /groups/{entraGroupId}/members",
    },
    {
      title: "Upsert members found in Entra",
      detail: "For each member: if not in FoundryGate DB → INSERT User (no APIM key yet — key is issued on first login). If already exists → update display fields.",
    },
    {
      title: "Deprovision members no longer in the Entra group",
      detail: "For each departed member: delete their APIM subscription, set User.IsActive = false, set QuotaAllocation.IsHardStopped = true.",
      code: "APIM Management: DELETE /subscriptions/{sid}\nUser.IsActive = false\nQuotaAllocation.IsHardStopped = true",
    },
  ]}
/>

<Aside type="caution">
**Departures are hard**: when a developer leaves the Entra group, their APIM subscription is *deleted* — not just suspended. Their key stops working immediately. This is intentional. Quota exhaustion only suspends; Entra departure permanently revokes.
</Aside>

### Bulk user sync

`POST /users/sync` pages through all users assigned to the FoundryGate service principal via Graph and upserts them. New users get a record with the default quota; departed users are deactivated. Useful for an initial import of your entire org before any developer has logged in.

---

## Five-level quota resolution

When a developer's allocation is created or updated, `IQuotaResolutionService` walks this chain — first match wins:

<QuotaChain />

The resolved value is written to `QuotaAllocation` for the current period. Admins can see which level resolved each developer's quota in the user detail view.

---

## APIM enforcement — how limits actually block traffic

FoundryGate doesn't rely on a database flag being checked on every request. The enforcement is APIM-native:

<Pipeline
  label="UsageSyncFunction — runs every 15 minutes"
  steps={[
    {
      title: "Query Log Analytics for token totals per APIM subscription",
      detail: "Aggregates token consumption for the current billing period across all APIM subscription IDs.",
    },
    {
      title: "Update QuotaAllocation.TokensUsed in SQL",
      detail: "Writes the latest consumption figures back to the database for UI display and audit.",
    },
    {
      title: "Suspend over-quota developers",
      detail: "For each developer where TokensUsed ≥ AllocatedTokens: call APIM Management PATCH to set subscription state to suspended, and set QuotaAllocation.IsHardStopped = true.",
      code: "APIM Management: PATCH /subscriptions/{sid}?state=suspended\nQuotaAllocation.IsHardStopped = true\n\nResult: APIM returns 401 immediately. No Foundry traffic passes.",
    },
  ]}
/>

There is up to a 15-minute window between hitting the limit and enforcement. This is intentional — querying Log Analytics on every request would add latency to every AI call.

<Pipeline
  label="MonthlyResetFunction — 00:01 UTC on the 1st of each month"
  steps={[
    {
      title: "Resolve quota for new period and insert QuotaAllocation",
      detail: "For each active developer, run the five-level quota resolution for the new month and INSERT a fresh QuotaAllocation row.",
    },
    {
      title: "Re-enable suspended subscriptions",
      detail: "For each developer whose subscription was suspended: call APIM Management PATCH → state: active. Set IsHardStopped = false.",
      code: "APIM Management: PATCH /subscriptions/{sid}\n  state: active\nQuotaAllocation.IsHardStopped = false",
    },
    {
      title: "Write audit log entry",
      detail: "Records the reset event as quota.monthly-reset in the AuditLog table.",
    },
  ]}
/>

---

## APIM key lifecycle

<KeyLifecycle />

**Suspend** — quota exhaustion: APIM subscription state → `suspended`. Key exists. Automatically re-enabled at monthly reset.

**Delete** — admin deactivation or Entra departure: APIM subscription deleted entirely. Key stops working immediately and cannot be restored without a full re-provision.

---

## Solution projects

| Project | Role |
|---|---|
| `FoundryGate.Api` | REST API, Azure Container App, Entra auth |
| `FoundryGate.Data` | EF Core entities, DbContext, migrations |
| `FoundryGate.Domain` | Shared DTOs and enums — no framework deps |
| `FoundryGate.Functions` | Timer-triggered Azure Functions (reset + usage sync) |
| `FoundryGate.Web` | Blazor WASM frontend (MudBlazor), Azure Static Web Apps |
| `FoundryGate.Database` | SQL Server project (.sqlproj) — builds dacpac for CI/CD |
| `FoundryGate.Cli` | `foundrygate` dotnet tool — schema compare, deploy, seed |
