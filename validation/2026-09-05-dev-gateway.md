# Dev gateway — live evidence, 2026-09-05

The first run of the gateway cycle against the **deployed dev environment** rather than a
throwaway one, plus the control-plane reconciliation path (#178) that only a deployed
environment can answer.

The sections up to *Reproducing this* are generated by `scripts/cycle/report.ps1` from the run's
state file and are re-rendered wholesale; everything after it was written by hand and would be
lost by a re-render. Generated by `scripts/cycle/cycle.ps1`. Every line below is an observation recorded during
the run, not a transcription. Re-render from the same state file with `scripts/cycle/report.ps1`.

## Summary

| | |
|---|---|
| Subscription | Imagile Paid |
| Resource group | rg-foundrygate-dev (eastus2) |
| Gateway URL | `https://apim-foundrygate-dev-e7k2.azure-api.net` |
| Anthropic front door | `https://apim-foundrygate-dev-e7k2.azure-api.net/anthropic` |
| OpenAI front door | `https://apim-foundrygate-dev-e7k2.azure-api.net/openai/v1` |
| APIM | `apim-foundrygate-dev-e7k2` |
| Tier products | `standard`, `power`, `unlimited` |
| Log Analytics workspace | `log-foundrygate-dev` (`062aeba1-151c-44c8-a554-1456d6d9fcc8`) |
| Foundry accounts | `fgdev-e7k2-eus2`, `fgdev-e7k2-swc` |
| Mode | **attached** to the already-deployed `dev` environment — nothing deployed, nothing torn down |
| Tier `standard` | 5000000 tokens/month, 20000 TPM |
| Tier `power` | 20000000 tokens/month, 40000 TPM |
| Tier `unlimited` | no monthly quota, 100000 TPM |
| Claude deployment | **not available** — see below |
| Teardown | not run |
| Checks | **20 PASS**, 0 FAIL, 7 SKIP |

## Checks

Raw header and body samples, keys redacted.

| ID | Check | Result | Evidence |
|---|---|---|---|
| `UP-3` | APIM diagnostic setting sends LLM logs to the dedicated table (#244) | **PASS** | 1 diagnostic setting(s), 1 with logAnalyticsDestinationType=Dedicated, 1 with GatewayLlmLogs enabled |
| `UP-1` | Gateway deployed | **PASS** | Attached to the existing 'dev' deployment foundrygate-dev (https://apim-foundrygate-dev-e7k2.azure-api.net); nothing was deployed. |
| `UP-2` | Anthropic (Claude) deployment provisioned | **SKIP** | No Anthropic deployment on 'dev'. This run made NO create attempt — attaching never deploys a model (#231, E-007). Claude-dependent checks report SKIP. |
| `SUB-1` | Developer keys issued against tier products | **PASS** | dev-alice=standard, dev-bob=standard, dev-carol=power on apim-foundrygate-dev-e7k2; all keys answering |
| `T3a` | OpenAI front door, no key | **PASS** | HTTP 401 — { "statusCode": 401, "message": "Access denied due to missing subscription key. Make sure to include subscription key wh... |
| `T3b` | OpenAI front door, bad key | **PASS** | HTTP 401 — { "statusCode": 401, "message": "Access denied due to invalid subscription key. Make sure to provide a valid key for an ... |
| `T3c` | Anthropic front door, no key | **PASS** | HTTP 401 — { "statusCode": 401, "message": "Access denied due to missing subscription key. Make sure to include subscription key wh... |
| `T3d` | Anthropic front door, bad key | **PASS** | HTTP 401 — { "statusCode": 401, "message": "Access denied due to invalid subscription key. Make sure to provide a valid key for an ... |
| `T2a` | POST /openai/v1/chat/completions with api-key -> 200 | **PASS** | HTTP 200, x-fg-remaining-tpm=39984, x-fg-tokens-consumed=16 |
| `T2b` | POST /openai/v1/responses with api-key -> 200 | **PASS** | HTTP 200, x-fg-remaining-tpm=39984 |
| `T1` | POST /anthropic/v1/messages with x-api-key -> 200 | **SKIP** | No Anthropic deployment reached Succeeded on this cycle (E-007). Not a gateway failure — the front door, key mapping and policy chain are exercised by T3c/T3d and A3. |
| `A1` | Unknown alias -> 403 model_not_permitted | **PASS** | HTTP 403, x-fg-error=model_not_permitted — {"error":{"code":"model_not_permitted","type":"invalid_request_error","param":"model","message":"Model 'no-such-alias' is not available on your FoundryGate plan... |
| `A2` | Real deployment name used as a model -> 403 model_not_permitted | **PASS** | HTTP 403, x-fg-error=model_not_permitted — {"error":{"code":"model_not_permitted","type":"invalid_request_error","param":"model","message":"Model 'gpt-4-1-mini' is not available on your FoundryGate plan.... |
| `A3` | Claude alias on the OpenAI front door -> 403 naming the Anthropic path | **PASS** | HTTP 403, x-fg-error=model_not_permitted — {"error":{"code":"model_not_permitted","type":"invalid_request_error","param":"model","message":"Model 'sonnet' is served by the anthropic front door on this gateway. Send this request to /anthropic i... |
| `T4a` | TPM cap returns 429 with Retry-After | **PASS** | gateway meter: HTTP 429 after 5 requests, Retry-After=10, x-fg-remaining-tpm=0 on the refusal — { "statusCode": 429, "message": "Token limit is exceeded. Try again in 10 seconds." } |
| `T4b` | Same-tier dev-bob unaffected while dev-alice is 429 (counter keyed on subscription) | **PASS** | dev-bob HTTP 200, x-fg-remaining-tpm=19984 while dev-alice is throttled |
| `T5a` | Monthly token quota exhausted -> 403 | **SKIP** | SKIPPED BY DESIGN, not by failure: exhausting the 5000000-token 'standard' budget through its own 20000 TPM cap takes at least 250 minutes of continuous full-rate traffic (cap: 20 min). The monthly counter is keyed on the APIM subscription and APIM offers no way to reset it, so burning it would also leave a spent counter on this environment until the calendar month rolls over. The 403 path is proved on the cycle's own gateway, whose standard tier is deployed deliberately small (see validation/2026-09-05-gateway-cycle.md, T5a-c); it is the same llm-token-limit element T4a/T4b exercise here, and x-fg-remaining-quota is observed counting down on this environment even though it does not reach zero. |
| `T5b` | x-fg-remaining-quota reached 0 before the refusal | **SKIP** | SKIPPED BY DESIGN, not by failure: exhausting the 5000000-token 'standard' budget through its own 20000 TPM cap takes at least 250 minutes of continuous full-rate traffic (cap: 20 min). The monthly counter is keyed on the APIM subscription and APIM offers no way to reset it, so burning it would also leave a spent counter on this environment until the calendar month rolls over. The 403 path is proved on the cycle's own gateway, whose standard tier is deployed deliberately small (see validation/2026-09-05-gateway-cycle.md, T5a-c); it is the same llm-token-limit element T4a/T4b exercise here, and x-fg-remaining-quota is observed counting down on this environment even though it does not reach zero. |
| `T5c` | Power-tier dev-carol still 200 while standard-tier dev-alice is quota-blocked | **SKIP** | SKIPPED BY DESIGN, not by failure: exhausting the 5000000-token 'standard' budget through its own 20000 TPM cap takes at least 250 minutes of continuous full-rate traffic (cap: 20 min). The monthly counter is keyed on the APIM subscription and APIM offers no way to reset it, so burning it would also leave a spent counter on this environment until the calendar month rolls over. The 403 path is proved on the cycle's own gateway, whose standard tier is deployed deliberately small (see validation/2026-09-05-gateway-cycle.md, T5a-c); it is the same llm-token-limit element T4a/T4b exercise here, and x-fg-remaining-quota is observed counting down on this environment even though it does not reach zero. |
| `T5d` | Monthly quota counter is live and decrementing (x-fg-remaining-quota) | **PASS** | dev-carol x-fg-remaining-quota 19999984 -> 19999968 across two requests against the standard/power products on this gateway |
| `C1` | codex exec completes against the gateway (power tier) | **PASS** | exit=0; stdout=pong; stderr=Reading prompt from stdin... OpenAI Codex v0.153.4 -------- workdir: C:\Code\foundry-gate\.claude\worktrees\agent-af86378fa7d6fdc2b\scripts\cycle\.state\codex-work-dev model: gpt provider: azure appro... |
| `C2` | Codex observed behaviour at the TPM 429 wall | **PASS** | after 3 exec(s): gateway 429 Retry-After=13, x-fg-remaining-tpm=0; codex exit=0; stderr=Reading prompt from stdin... OpenAI Codex v0.153.4 -------- workdir: C:\Code\foundry-gate\.claude\worktrees\agent-af86378fa7d6fdc2b\scripts\cycle\.state\codex-work-dev model: gpt provider: azure approval: never sandbox: read-only reasoning ... |
| `C3` | Codex observed behaviour at the monthly-quota 403 wall | **SKIP** | SKIPPED BY DESIGN on this environment: the 'power' tier's 20000000-token budget needs at least 500 minutes of continuous full-rate traffic to exhaust (cap: 30 min), which is hours of codex exec and several dollars of real tokens. The monthly counter cannot be reset once spent, so reaching it here would also leave the tier exhausted for anyone else on it until the month rolls over. C3 is proved on the cycle's own gateway, whose power tier is deployed deliberately small — see validation/2026-09-05-gateway-cycle.md. |
| `C4` | claude -p completes against the gateway (Foundry mode) | **SKIP** | No Anthropic deployment reached Succeeded on this cycle (E-007), so the Claude Code env-var block is not verified by this run. |
| `M1` | ApiManagementGatewayLlmLog rows present in Log Analytics | **PASS** | 62 log entries across 62 distinct CorrelationIds in 2026-09-05T14:04:36Z/2026-09-05T18:04:36Z |
| `M2` | Reconciliation KQL returns per-developer totals | **PASS** | fgcycle-carol-202609051749=9449 tokens/18 req; fgcycle-alice-202609051749=48590 tokens/9 req; fgcycle-bob-202609051749=28751 tokens/10 req; foundrygate-9=13124 tokens/13 req |
| `M3` | D-017 de-duplication assumption checked against live data | **PASS** | NOT EXERCISED on this traffic: every one of 62 CorrelationIds produced exactly one entry, so max() and sum() agree (99914 tokens). The de-duplication is still correct, but this cycle's request shapes (small, non-streamed) did not produce the chunked multi-entry case the D-017 note is about. |

## Model deployments

| Account | Deployment | Format | Provisioning state |
|---|---|---|---|
| `fgdev-e7k2-eus2` | `claude-sonnet-4-5` | Anthropic | Failed |

> **Anthropic provisioning (E-007).** This cycle made exactly ONE Claude create attempt,
> through the day-0 ARM deploy, and it did not reach `Succeeded`. It was **not** retried and
> the deployment was **not** deleted and recreated: churn is what poisons an account for
> every later attempt. The demo continued on the OpenAI path, which is the primary harness
> path anyway (Codex).

## Measurement — `UsageBySubscription.kql` against the live workspace

| APIM subscription | Prompt | Completion | Total | Requests |
|---|---|---|---|---|
| `fgcycle-carol-202609051749` | 9428 | 21 | 9449 | 18 |
| `fgcycle-alice-202609051749` | 42186 | 6404 | 48590 | 9 |
| `fgcycle-bob-202609051749` | 27971 | 780 | 28751 | 10 |
| `foundrygate-9` | 12293 | 831 | 13124 | 13 |

### D-017 / #178 — the `max()` de-duplication assumption

- Distinct `CorrelationId`s: **62**
- With more than one log entry: **0** (max 1 entries for a single request)
- Naive `sum()` over every entry: **99914** tokens
- De-duplicated (`max()` per CorrelationId, then sum): **99914** tokens
- Inflation a naive sum would have introduced: **1x**

## Reproducing this

```sh
pwsh scripts/cycle/cycle.ps1 -Subscription "Imagile Paid" -Environment dev -CleanupSubscriptions
```

See `.claude/skills/gateway-cycle/SKILL.md` for the unattended runbook, the Anthropic
create-once rules, and what each failure mode means.



---

*Everything below is hand-written and is not regenerated by `report.ps1`.*

## What running against a shared environment changed

| | `test` (the cycle owns it) | `dev` (attached) |
|---|---|---|
| Gateway | deployed by the run, torn down after | already there; **nothing deployed, nothing torn down** |
| Tier limits | chosen by the run, deliberately tiny | read off the live product policies — 5M/20K, 20M/40K, unlimited/100K |
| 403 monthly wall | proved (`T5a-c`) | **SKIP by design** — 5 000 000 tokens behind 20 000 TPM is 250 minutes of full-rate traffic, and the counter cannot be reset |
| Standing in for it | — | `T5d`: `x-fg-remaining-quota` observed decrementing, 19 999 984 → 19 999 968 |
| Fixture keys | `fgcycle-*`, deleted at the end | same, and it matters — dev's APIM also carries the real `foundrygate-{UserId}` keys |

## Two things dev was quietly getting wrong

### 1. Every model 404'd (#259)

The first request through dev's gateway came back `404 DeploymentNotFound`. `fgdev-e7k2-eus2` held
one deployment — `claude-sonnet-4-5`, **Failed** (#231) — and `fgdev-e7k2-swc` held none, while the
alias map advertised `gpt → gpt-4-1-mini` on all three tiers. Dev's gateway had been answering 404 on
every model on every tier, with `Deploy All` green and every resource healthy.

`createModelDeployments = false` is permanent on dev (correctly — Anthropic deployments are
create-once, E-007), and nothing else owns creating the **OpenAI** deployment. Created out of band,
which is the documented-safe exception (E-007e). No Anthropic create was attempted.

### 2. The developer's own TPM meter was unreachable (#260)

The first 429 on dev looked like a pass and was not:

```
HTTP 429  Retry-After: 30  x-fg-remaining-tpm: 6413
{ "error": { "message": "Your requests to gpt-4.1-mini for gpt-4-1-mini in eastus2 have exceeded rate limit." } }
```

6 413 tokens of headroom on the refusal: that is Azure OpenAI's deployment saturating, not the
developer's budget. `gpt-4-1-mini` was 10 capacity units (~10K TPM) behind a 20 000 TPM tier, so the
backend wall sat **in front of** the gateway wall. Raised to 60 units; the gateway's own meter is now
reachable and `T4a` refuses to pass on someone else's 429 (`x-fg-remaining-tpm=0` on the refusal, or
it reports SKIP naming both numbers).

### And one that had never worked at all (#261)

`UsageSyncFunction` fires on schedule every 15 minutes on dev and **had failed every single time since
the app was deployed** — five for five in the six-hour window, ~35ms each, dying at binding time before
any user code ran:

```
Method not found: 'Void Microsoft.Azure.Functions.Worker.DefaultTraceContext..ctor(System.String, System.String)'
```

`Microsoft.Azure.Functions.Worker` 2.1.0 pins Core and Grpc to 2.1.0;
`Microsoft.Azure.Functions.Worker.OpenTelemetry` 1.2.0 independently requires Core >= 2.52.0.
Highest-wins lifted **Core** to 2.52.0 and left **Grpc** at 2.1.0, and Grpc 2.1.0 calls a constructor
Core 2.52.0 removed. The build was clean, 0 warnings, all tests green — the break is between two
transitive assemblies at runtime.

Fixed by bumping `Microsoft.Azure.Functions.Worker` to 2.52.0 so the two move together, and **verified
live**: the first tick after `Deploy All` landed the new build succeeded, and reconciliation ran end to
end. Numbers below.

## #178 — the reconciliation path, end to end

### The KQL half: proved exactly

A key was provisioned through the **real control-plane flow** (`foundrygate-9`, issued by
`POST /api/v1/keys/{userId}/provision`, revealed via `POST /api/v1/keys/me/reveal`) and driven through
the gateway. Twelve small requests, then one deliberately large one:

| Source | Requests | Tokens |
|---|---|---|
| Gateway response headers (`x-fg-tokens-consumed`, summed) | 12 | **1 095** |
| `x-fg-remaining-quota` delta (5 000 000 -> 4 998 905) | 12 | **1 095** |
| `UsageBySubscription.kql` for `foundrygate-9` | 12 | **1 095** |
| ...plus one 54 118-byte request (`x-fg-tokens-consumed=12029`) | 13 | **13 124** |

Three independent readings of the same traffic agreeing to the token. Against #178's checklist:

- **It parses, and the join matches.** `RequestCount` non-zero on all four subscriptions.
- **`ApimSubscriptionId` renders as the subscription NAME** — `foundrygate-9`, not an ARM id and not a
  display name. This is the one that decided whether `ApimSubscriptionNames.TryGetUserId` works at all,
  and it does. The `fgcycle-*` fixtures show up next to it as exactly what the job calls unknown
  subscriptions.
- **The `max()` de-duplication (D-017 / 1a): NOT EXERCISED, and now for a stronger reason than last
  time.** All 62 CorrelationIds produced exactly one entry, so `max()` and `sum()` agree
  (`NaiveSum == DedupedSum`, inflation 1x). #178 asks specifically for a >32 KB request, which forces
  chunking — one was sent, **54 118 bytes**, and it produced a *single* row: `SequenceNumber = 0`,
  `TotalTokens = 12029`, matching `x-fg-tokens-consumed` exactly. So on this gateway even the shape the
  D-017 note is about does not split, and the "still yields one correct total, not a multiple"
  checkbox is satisfied. `Entries > 1` remains unobserved; the de-duplication is defending against
  something this workspace has not yet produced.

### The Function half: blocked for hours, then proved in one tick

Everything the job needs was already correct on the deployed app:

- `Gateway__LogAnalyticsWorkspaceId` = `062aeba1-151c-44c8-a554-1456d6d9fcc8` — the **GUID**, not the
  ARM id (which is separately present as `Gateway__LogAnalyticsWorkspaceResourceId`). The host starts,
  so startup validation passed.
- All three functions enabled; the schedule log prints `Cron: '0 0,15,30,45 * * * *'` and the next five
  occurrences.
- The APIM diagnostic setting carries `logAnalyticsDestinationType: Dedicated` with `GatewayLlmLogs`
  enabled — the #244 fix is in the deployed template, and `UP-3` now checks it on every attach so a
  regression is caught at attach time instead of after a four-hour ingestion hunt.

And it had never run, for #261 — every invocation since the app was deployed died at binding time.
`Deploy All` [run 33983870847](https://github.com/kolatts/foundry-gate/actions/runs/33983870847) landed
the fixed worker at 18:44Z, and the very next tick was the first success in the app's life:

| TimeGenerated | Success | Duration |
|---|---|---|
| 17:45 / 18:00 / 18:15 / 18:30 | **False** | 37 / 42 / 61 / 71 ms |
| **18:45** | **True** | **2 943 ms** |

The duration is its own evidence: tens of milliseconds is a `MissingMethodException` thrown before user
code; ~3 seconds is the job querying Log Analytics and writing to SQL.

**Four independent readings of the same traffic, all agreeing to the token:**

| Source | `foundrygate-9` |
|---|---|
| `x-fg-tokens-consumed` headers, summed | 13 124 *(my traffic)* |
| `x-fg-remaining-quota` delta | 13 124 |
| `UsageBySubscription.kql`, at 18:47 | **13 220** |
| `QuotaAllocation.TokensUsed`, reconciled at 18:45 | **13 220** |

The last two include six further requests from the pipeline's own postdeployment tests, which ran
between the two measurements. The point is that the KQL and the database agree **exactly**, on a value
that was `13 220` and `0` respectively an hour earlier — and they went on agreeing: the 19:00 pass moved
both to `13 252`.

One `usage.synced` audit row, whose counts describe the environment correctly:

```json
{"subscriptionsSeen":9,"allocationsUpdated":3,"unknownSubscriptions":6,
 "missingAllocations":0,"driftCount":0,"periodYear":2026,"periodMonth":9,
 "periodsReconciled":["2026-09"]}
```

`allocationsUpdated: 3` is `foundrygate-1`, `-2` and `-9` — every real developer key in the workspace.
`unknownSubscriptions: 6` is exactly the six `fgcycle-*` cycle fixtures, whose Log Analytics rows
outlive the subscriptions themselves. So `ApimSubscriptionNames.TryGetUserId` partitions real
developers from everything else correctly, on real data, which was the mapping question #178 existed to
answer. The Functions identity's `Log Analytics Reader` assignment is confirmed by the same run — the
job read the workspace with no 403.

The 19:00 tick also succeeded (5 512 ms) and wrote a **second** `usage.synced` row —
`allocationsUpdated: 1`, `tokensUsed` 13 220 → 13 252. That is the guard behaving correctly rather than
the guard failing: something genuinely moved, and D-016 audits when it does. **The idle branch is still
unobserved** — dev has had traffic on every pass so far, so "two hours of ticks with no traffic produce
~1 row, not 8" remains an open item on #178, not something this run can claim.

### What #178 still does not have

`MonthlyQuotaResetFunction` and `EntraSyncFunction` share the worker and were failing identically, so
they are unblocked — but neither has been *observed* running (daily 00:01 UTC and nightly). Section 3
of #178 (the reset, its blob lease, missed-day recovery) and section 4 (tier drift) are untouched, as
are the longer-horizon items in section 2: the **idle-guard soak** (two hours of ticks with no traffic
→ ~1 row, not 8), a deliberate over-budget drift row, and the previous-period catch-up, which is only
checkable on the 1st–3rd of a month.

### The finding worth more than the fix

Five consecutive failed invocations produced **no alert and no symptom anywhere an operator looks**: the
host reported `Running`, all three functions reported enabled, the schedule log printed its next five
occurrences correctly, `/health/ready` was green, and `Deploy All` — including its postdeployment tests
— passed. Meanwhile `QuotaAllocation.TokensUsed` was `0` for every developer while the gateway and Log
Analytics both held the true numbers, so every usage dashboard was wrong with nothing saying so.

A timer that fails every tick is indistinguishable from a timer that has nothing to do. Filed as #266.

## What #237's floor looks like from the other side

dev ships `standard` at 20 000 TPM, which is *above* one agent turn. Three consecutive `codex exec`
runs as a standard-tier developer:

| exec | codex exit | gateway after | tokens spent |
|---|---|---|---|
| 1 | **0** | 200, tpm=10 660 | 9 574 |
| 2 | **0** | 200, tpm=3 048 | 9 574 |
| 3 | **0** | **429**, tpm=0, Retry-After=13 | 9 574 |

Every exec **completed**, and the 429 arrived cleanly between execs. Compare the test stack at 8 000 and
12 000 TPM, where the same CLI against the same policy exited 1 for twenty-five consecutive runs without
finishing one (#237). Same gateway, same harness — the only variable is whether the per-minute cap
clears one agent turn. And `codex exec` costs **9 574 tokens**, three times, to the token.

## Cleanup

The `fgcycle-*` fixture subscriptions were removed with
`pwsh scripts/cycle/subscriptions.ps1 -Environment dev -Cleanup`. `foundrygate-9` is a real
control-plane key and was left in place. Nothing was torn down: `down.ps1` refuses dev, and
`infra-destroy.yml` is the only sanctioned path.
